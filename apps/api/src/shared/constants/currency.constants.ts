/**
 * The hryvnia, as ISO 4217 numbers it.
 *
 * One home because it had three: the matching rules, PrivatBank's adapter and
 * monobank's each stated `980` for themselves, which is three chances for one
 * of them to state something else. Nothing about it is receipt-specific — it is
 * a fact about the currency this product settles in.
 */
export const UAH_CURRENCY_CODE = 980
