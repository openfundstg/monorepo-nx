/**
 * The product's mark, served from `public/` and so referenced without a prefix.
 *
 * Cut from the artwork with its black tile removed, so the cream glyph sits on
 * whichever surface the panel puts behind it rather than punching a square hole
 * in it.
 */
export const BRAND_MARK_SRC = 'logo.png';

/** What it is announced as where no visible name sits beside it. */
export const BRAND_NAME = 'Open Funds';
